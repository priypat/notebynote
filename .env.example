# Mocks read from lib/mock/ and persist writes to localStorage.
# Set to false once a real backend exists — see API_CONTRACT.md.
NEXT_PUBLIC_USE_MOCKS=true

# Base URL for the real API. Empty means same-origin.
NEXT_PUBLIC_API_BASE=
